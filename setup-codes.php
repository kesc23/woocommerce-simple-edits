<?php

if ( ! defined( 'ABSPATH' ) ) {
	exit; // Exit if accessed directly
}

require_once 'kesc-function.php';


/**
 *esse código é para definir o id dos shortcodes que serão exibidos ANTES do breadcrumb e ANTES dos filtros
 *cada um com sua especificação
 */

//ID PARA O ELEMENTO QUE VAI ANTERIOR AO BREADCRUMB
$catshtcd = 255;
 
//ID PARA O ELEMENTO QUE VAI ANTERIOR AO FILTRO
$catslide = 257;